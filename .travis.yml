language: python
cache: pip
python:
    - "3.4"
    - "3.5"
    - "3.6"
    - "3.6-dev"

install:
    - pip install .[dev]
    - pip install git+https://gitlab.kwant-project.org/cwg/wraparound.git

# run tests
script:
    - cd tests
    - py.test
